import React from 'react'

function NotFound(props) {
    // const {history} = props;
    // setTimeout( () => {
    //     history.push('/');
    // }, 5000);
    
    return (
        <div>
            The Page Didn't Found!
        </div>
    )
}

export default NotFound;
